# Extension examples

See [the example guide](../docs/docs/extensions/examples.md) for all five extensions, builds and installation.

Shared UI support is part of the base mod at `wtf.tatp.meowtils.extension.render`. Each archive contains its own code and assets only.
