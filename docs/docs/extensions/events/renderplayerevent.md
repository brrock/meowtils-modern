# RenderPlayerEvent

Not posted on 26.2. There is no per-player render event on the Meowtils bus. Use `RenderWorldLastEvent` / `WorldOverlay` for world-space draws, or a Fabric render callback in a real Fabric mod if you need mixins.
